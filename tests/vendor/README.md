# Vendored third-party test dependency

doctest v2.4.11 - MIT licensed - https://github.com/doctest/doctest

Single-header unit-test framework. Used ONLY by the test suite; the
simulator core (rp2040_core) stays dependency-free per CLAUDE.md.
